export const DEFAULT_SERVER_ORIGIN = 'http://10.10.7.33:5001';
// Always use backend origin, not the static page origin
export const SERVER_ORIGIN = DEFAULT_SERVER_ORIGIN;
export const API_BASE = SERVER_ORIGIN + '/api/v1';